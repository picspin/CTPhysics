name: Vercel Deployment Marker

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Production deployment is handled by Vercel Git integration. GitHub Pages deployment is intentionally disabled."
